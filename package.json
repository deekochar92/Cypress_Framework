{
  "name": "miro_assignment",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "npx cypress run --browser chrome --headed --env allure=true",
    "allure:report": "allure generate allure-results --clean -o allure-report",
    "test": "npm run cy:run || npm run allure:report "
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^7.6.0",
    "cypress": "^11.2.0"
  },
  "dependencies": {
    "@shelex/cypress-allure-plugin": "^2.34.0",
    "allure-commandline": "^2.20.1"
  }
}